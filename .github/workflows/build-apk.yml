name: 构建 Android APK

on:
  push:
    tags:
      - 'v*'  # 当推送 v 开头的 tag 时触发
  workflow_dispatch:  # 支持手动触发

jobs:
  build-apk:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
      
      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: 设置 Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: 设置 Android SDK
        uses: android-actions/setup-android@v3
      
      - name: 安装依赖
        run: npm install
      
      - name: 构建 Web 版本
        run: npm run build:web
      
      - name: 初始化 Capacitor Android
        run: |
          if [ ! -d "android" ]; then
            npx cap add android
          fi
      
      - name: 生成 Android 图标
        run: |
          # 安装 ImageMagick
          sudo apt-get update && sudo apt-get install -y imagemagick
          
          # 源图标
          SRC_ICON="edge-extension/icons/icon-256.png"
          
          # 生成各尺寸图标
          convert "$SRC_ICON" -resize 48x48 android/app/src/main/res/mipmap-mdpi/ic_launcher.png
          convert "$SRC_ICON" -resize 72x72 android/app/src/main/res/mipmap-hdpi/ic_launcher.png
          convert "$SRC_ICON" -resize 96x96 android/app/src/main/res/mipmap-xhdpi/ic_launcher.png
          convert "$SRC_ICON" -resize 144x144 android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png
          convert "$SRC_ICON" -resize 192x192 android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png
          
          # 圆形图标（如果需要）
          convert "$SRC_ICON" -resize 48x48 android/app/src/main/res/mipmap-mdpi/ic_launcher_round.png
          convert "$SRC_ICON" -resize 72x72 android/app/src/main/res/mipmap-hdpi/ic_launcher_round.png
          convert "$SRC_ICON" -resize 96x96 android/app/src/main/res/mipmap-xhdpi/ic_launcher_round.png
          convert "$SRC_ICON" -resize 144x144 android/app/src/main/res/mipmap-xxhdpi/ic_launcher_round.png
          convert "$SRC_ICON" -resize 192x192 android/app/src/main/res/mipmap-xxxhdpi/ic_launcher_round.png
          
          # 前景图标（自适应图标）
          convert "$SRC_ICON" -resize 108x108 android/app/src/main/res/mipmap-mdpi/ic_launcher_foreground.png
          convert "$SRC_ICON" -resize 162x162 android/app/src/main/res/mipmap-hdpi/ic_launcher_foreground.png
          convert "$SRC_ICON" -resize 216x216 android/app/src/main/res/mipmap-xhdpi/ic_launcher_foreground.png
          convert "$SRC_ICON" -resize 324x324 android/app/src/main/res/mipmap-xxhdpi/ic_launcher_foreground.png
          convert "$SRC_ICON" -resize 432x432 android/app/src/main/res/mipmap-xxxhdpi/ic_launcher_foreground.png
          
          echo "✅ Android 图标生成完成"
      
      - name: 同步资源
        run: npx cap sync android
      
      - name: 设置 Gradle 权限
        run: chmod +x android/gradlew
      
      - name: 构建 Debug APK
        run: |
          cd android
          ./gradlew assembleDebug --no-daemon
      
      - name: 获取版本号
        id: version
        run: |
          VERSION=$(node -p "require('./edge-extension/manifest.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "📦 版本号: v$VERSION"
      
      - name: 重命名 APK
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          mkdir -p dist
          cp "android/app/build/outputs/apk/debug/app.apk" "dist/IdoFront-v${VERSION}.apk"
          echo "📱 APK 文件: dist/IdoFront-v${VERSION}.apk"
          ls -lh dist/
      
      - name: 上传 APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: IdoFront-APK
          path: dist/*.apk
          retention-days: 30
      
      # 如果是 tag 推送，创建 GitHub Release
      - name: 创建 GitHub Release
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v1
        with:
          name: IdoFront ${{ github.ref_name }}
          body: |
            ## IdoFront ${{ github.ref_name }}
            
            ### 📱 下载
            - **Android APK** (Debug 版本): 下载下方的 APK 文件
            - **浏览器扩展**: 下载 Extension zip 包
            
            ### ⚠️ 安装说明
            **Android APK:**
            1. 下载 APK 文件
            2. 在 Android 设备上打开进行安装
            3. 如果提示"未知来源"，请在设置中允许安装
            
            **浏览器扩展:**
            1. 下载 Extension zip 包并解压
            2. 打开浏览器扩展管理页面
            3. 开启"开发者模式"
            4. 点击"加载已解压的扩展"选择解压目录
          files: |
            dist/*.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
